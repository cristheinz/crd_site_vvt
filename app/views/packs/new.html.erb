<% provide(:title, t(:administration)) %>
<div class="row">
	<div class="span12">
		<div class="inner-box">
			<h1><%=t(:packages)%></h1>
			<legend><%=t(:new_pack)%></legend>

			<%= render 'form' %>

		</div>
	</div>
</div>